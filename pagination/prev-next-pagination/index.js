let list = new Array(); // collection of data to be sorted
let pageList = new Array(); // collection presently on the page
let currentPage = 1; // current page
let numberPerPage = 10; // number of items per page
let numberOfPages = 1; // calculates the total number of pages
